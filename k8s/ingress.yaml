apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-210216-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-210216.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-210216-service
            port:
              number: 80
